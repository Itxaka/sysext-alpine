# Build output (root-level only — examples/ contains a usr/bin payload)
/bin/

# Test extension images
*.raw
# ... except the committed signed example (public test keys, see examples/)
!examples/signed-example.raw

# Go test artifacts
*.test
*.out
coverage.html

# Local test fixtures (real signed images, certificates, PRIVATE KEYS —
# never commit; see test/fixtures/README.md)
test/fixtures/*
!test/fixtures/README.md

# Coverage artifacts
coverage*.out
coverage.html
.covdata/
