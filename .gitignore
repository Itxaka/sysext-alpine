# Build output (root-level only — examples/ contains a usr/bin payload)
/bin/

# Test extension images (the signed example is regenerated, not committed:
# run examples/make-signed-sysext.sh or `make example`)
*.raw

# Go test artifacts
*.test
*.out
coverage.html

# Local test fixtures (real signed images, certificates, PRIVATE KEYS —
# never commit; see test/fixtures/README.md)
test/fixtures/*
!test/fixtures/README.md

# Coverage artifacts
coverage*.out
coverage.html
.covdata/

# Stray build binary (committed by accident once)
/sysext
/confext
